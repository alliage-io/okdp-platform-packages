#
# Copyright 2026 The OKDP Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# you may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS,
#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#  See the License for the specific language governing permissions and
#  limitations under the License.
#

apiVersion: v1alpha1
name: seaweedfs
tag: 4.17.0-p1
protected: false
description: |
  SeaweedFS is a simple and highly scalable distributed file system. There are the objectives to store billions of files and to serve the files fast!
usage:
  text: |
    SeaweedFS is a simple and highly scalable distributed file system.
    Access the console at {{ .Context.seaweedfs.endpoint.filerUrl | replace "{{ .Release.namespace }}" .Release.spec.targetNamespace | replace "{{ .Release.metadata.name }}" .Release.metadata.name | replace "{{ .Context.ingress.suffix }}" .Context.ingress.suffix }}
schema:
  parameters:
    properties:
      username: { type: string, required: true, default: "admin" }
      password: { type: string, required: true, default: "admin123" }
      volumeReplicas: { type: integer, required: true, default: 1 }
      masterStorage: { type: string, required: true, default: "1" }
      masterLogStorage: { type: string, required: true, default: "0.5" }
      volumeStorage: { type: string, required: true, default: "2" }
      filerStorage: { type: string, required: true, default: "0.5" }
      filerLogStorage: { type: string, required: true, default: "0.5" }
  context:
    properties:
      certificateIssuers:
        required: true
        properties:
          selfSigned:
            properties:
              name: { type: string, required: true }
      storageClass:
        required: true
        properties:
          data: { type: string, required: true }
      ingress:
        required: true
        properties:
          suffix: { type: string, required: true }
modules:
  - name: auth-config
    source:
      local:
        path: ./auth-config
    values: |

      iamConfig:
        sts:
          tokenDuration: "1h"
          maxSessionLength: "12h"
          issuer: "seaweedfs-sts"
          signingKey: ""
      
        # -- IAM policies to define in the SeaweedFS IAM config.
        policies:
          - name: "S3WritePolicy"
            document:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action: ["s3:List*","s3:Get*","s3:Put*","s3:DeleteObject"]
                  Resource: ["*"]
        
        # -- IAM roles to define in the SeaweedFS IAM config.
        roles:
          - roleName: "S3WriteRole"
            roleArn: "{{ .Context.seaweedfs.s3.sts.roleArn }}"
            attachedPolicies: ["S3WritePolicy"]
            trustPolicy:
              Version: "2012-10-17"
              Statement:
                # Polaris uses Credentials-based STS AssumeRole
                - Effect: "Allow"
                  Principal: "*"
                  Action:
                    - "sts:AssumeRole"
      
      s3Config:
        enabled: true
        secretName: {{ .Context.seaweedfs.s3.s3_secret }}
        identities:
          - name: {{ .Context.seaweedfs.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.seaweedfs.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.seaweedfs.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.seaweedfs.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.seaweedfs.s3.providerConfig.seaweedfs.actions | default list | toJson }}
          - name: {{ .Context.hiveMetastore.storage.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.hiveMetastore.storage.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.hiveMetastore.storage.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.hiveMetastore.storage.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.hiveMetastore.storage.s3.providerConfig.seaweedfs.actions | default list | toJson }}
          - name: {{ .Context.trino.storage.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.trino.storage.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.trino.storage.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.trino.storage.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.trino.storage.s3.providerConfig.seaweedfs.actions | default list | toJson }}
          - name: {{ .Context.jupyterHub.storage.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.jupyterHub.storage.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.jupyterHub.storage.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.jupyterHub.storage.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.jupyterHub.storage.s3.providerConfig.seaweedfs.actions | default list | toJson }}
          - name: {{ .Context.sparkHistory.storage.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.sparkHistory.storage.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.sparkHistory.storage.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.sparkHistory.storage.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.sparkHistory.storage.s3.providerConfig.seaweedfs.actions | default list | toJson }}
          - name: {{ .Context.examples.storage.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.examples.storage.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.examples.storage.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.examples.storage.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.examples.storage.s3.providerConfig.seaweedfs.actions | default list | toJson }}
          # The Polaris SeaweedFS identity for STS
          - name: {{ .Context.polaris.storage.s3.providerConfig.seaweedfs.identity }}
            credentialsSecret:
              name: {{ .Context.polaris.storage.s3.credentialsSecret.name }}
              accessKeyKey: {{ .Context.polaris.storage.s3.credentialsSecret.accessKeyKey }}
              secretKeyKey: {{ .Context.polaris.storage.s3.credentialsSecret.secretKeyKey }}
            actions: {{ .Context.polaris.storage.s3.providerConfig.seaweedfs.actions | default list | toJson }}

      filer:
        auth:
          method: basic
          basic:
            create_auth_secret: creds-seaweedfs-filer-basic
            username: {{ .Parameters.username }}
            password: {{ .Parameters.password }}

  - name: main
    dependsOn:
      - auth-config
    timeout: 10m
    source:
      helmRepository:
        url: https://seaweedfs.github.io/seaweedfs/helm
        chart: seaweedfs
        version: 4.17.0
    values: |
      {{- $seaweedfsS3Url := .Context.seaweedfs.endpoint.s3Url | replace "{{ .Release.namespace }}" .Release.spec.targetNamespace -}}
      {{- $seaweedfsS3Url := $seaweedfsS3Url | replace "{{ .Release.metadata.name }}" .Release.metadata.name -}}
      {{- $seaweedfsS3Url := $seaweedfsS3Url | replace "{{ .Context.ingress.suffix }}" .Context.ingress.suffix -}}
      {{- $seaweedfsFilerUrl := .Context.seaweedfs.endpoint.filerUrl | replace "{{ .Release.namespace }}" .Release.spec.targetNamespace -}}
      {{- $seaweedfsFilerUrl := $seaweedfsFilerUrl | replace "{{ .Release.metadata.name }}" .Release.metadata.name -}}
      {{- $seaweedfsFilerUrl := $seaweedfsFilerUrl | replace "{{ .Context.ingress.suffix }}" .Context.ingress.suffix -}}
      
      {{- $seaweedfsS3Host := regexReplaceAll "^https?://" $seaweedfsS3Url "" -}}
      {{- $seaweedfsFilerHost := regexReplaceAll "^https?://" $seaweedfsFilerUrl "" -}}
      
      nameOverride: "{{ .Release.metadata.name }}"
      fullnameOverride: "{{ .Release.metadata.name }}"

      global:
        imageName: chrislusf/seaweedfs
      
      image:
        tag: "4.17"

      master:
        enabled: true
        replicas: 1
        data:
          type: "persistentVolumeClaim"
          storageClass: "{{ .Context.storageClass.workspace }}"
          size: "{{ .Parameters.masterStorage }}Gi"

        logs:
          type: "persistentVolumeClaim"
          storageClass: "{{ .Context.storageClass.workspace }}"
          size: "{{ .Parameters.masterLogStorage }}Gi"
      
      volume:
        enabled: true
        replicas: {{ .Parameters.volumeReplicas }}
        dataDirs:
          - name: data
            type: "persistentVolumeClaim"
            storageClass: "{{ .Context.storageClass.workspace }}"
            size: "{{ .Parameters.volumeStorage }}Gi"
            maxVolumes: 0

      filer:
        enabled: true
        replicas: 1
        enablePVC: true
        port: 8888
        data:
          type: "persistentVolumeClaim"
          storageClass: "{{ .Context.storageClass.workspace }}"
          size: "{{ .Parameters.filerStorage }}Gi"
        logs:
          type: "persistentVolumeClaim"
          storageClass: "{{ .Context.storageClass.workspace }}"
          size: "{{ .Parameters.filerLogStorage }}Gi"
        ingress:
          enabled: true
          className: nginx
          annotations:
            nginx.ingress.kubernetes.io/proxy-body-size: "130m"
            cert-manager.io/cluster-issuer: "{{ .Context.certificateIssuers.selfSigned.name }}"
            nginx.ingress.kubernetes.io/auth-type: basic
            nginx.ingress.kubernetes.io/auth-secret: creds-seaweedfs-filer-basic
            nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
          host: {{ $seaweedfsFilerHost }}
          path: "/"
          pathType: Prefix
          tls:
            - secretName: seaweedfs-filer-tls
              hosts:
                - {{ $seaweedfsFilerHost }}
      
      s3:
        enabled: {{ .Context.seaweedfs.s3.enabled }}
        enableAuth: true
        existingConfigSecret: {{ .Context.seaweedfs.s3.s3_secret }}
        createBuckets:
          - name: {{ .Context.hiveMetastore.storage.s3.bucket }}
            anonymousRead: {{ .Context.hiveMetastore.storage.s3.bucketAnonymousRead }}
          - name: {{ .Context.sparkHistory.storage.s3.bucket }}
            anonymousRead: {{ .Context.sparkHistory.storage.s3.bucketAnonymousRead }}
      
        extraVolumes: |
          - name: auth-config
            configMap:
              name: {{ .Release.metadata.name }}-auth-config
  
        extraVolumeMounts: |
          - name: auth-config
            mountPath: /etc/seaweed/iam
            readOnly: true
        {{- if .Context.seaweedfs.s3.sts.enabled }}
        extraArgs:
          - "-iam.config=/etc/seaweed/iam/iam.json"
        {{- end }}
      
        ingress:
          enabled: true
          className: nginx
          annotations:
            nginx.ingress.kubernetes.io/proxy-body-size: "130m"
            cert-manager.io/cluster-issuer: "{{ .Context.certificateIssuers.selfSigned.name }}"
          host: {{ $seaweedfsS3Host }}
          path: "/"
          pathType: Prefix
          tls:
            - secretName: seaweedfs-s3-tls
              hosts:
              - {{ $seaweedfsS3Host }}

roles:
  - storage
dependencies:
  - ingress
  # - database-server
